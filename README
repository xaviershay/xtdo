Hax!

  alias t=./t
  source completion # zsh only
  t add some task  # Add to list
  t bump 0 s<TAB>  # Move task to TODAY
  t a 0 today task # New task on today list, shortcut letter
  t done t<TAB>    # It is done!
  t list           # Today's tasks
  t list all       # All tasks
