Hax!

  alias t=./t
  source completion  # zsh only

  t add some task    # Add to list
  t bump 0 s<TAB>    # Move task to TODAY
  t a 0 another task # New task on today list, shortcut letter
  t list             # Today's tasks
  t done a<TAB>      # It is done!
  t b 1w s<TAB>      # Actually we will to it next week
  t l all            # All tasks
