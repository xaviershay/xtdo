#!/usr/bin/env ruby

$LOAD_PATH.unshift File.expand_path("../../lib", __FILE__)

require 'xtdo'

result = Xtdo.run "~/.xtdo", ARGV.join(" ")
puts result if result && result.is_a?(String) && result.length > 0
